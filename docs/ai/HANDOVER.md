# Handover

## Current state
- Working: **Phase 3 complete — server-authoritative liquidation + shareable result cards.** The world tick loop now force-closes player funds that breach margin (server-side, not client-triggered), marks them `liquidated` in Postgres, writes a `LiquidationLog` with rival context, and the `GET /funds/:id/result-card` (JSON) and `GET /funds/:id/result-card.png` (1200×630 PNG via `@vercel/og`) endpoints are live. Frontend `FundSummaryPanel` (SHARE button) is accessible any time; `LiquidationScreen` now shows the rival-context line and a SHARE RESULT button. 84/84 tests pass across 7 files.
- In progress: **Live "world ticks unattended" test still BLOCKED by this machine** (WSL/Docker). Same blocker as Phase 4 — `wsl --install -d Ubuntu` + reboot needed.
- Broken: Nothing in code. All static checks green: root tsc PASS, server tsc PASS, 84/84 tests, lint PASS (max-warnings 0), client build ✓ 1.53s, prisma generate ✓.
- Avoid: Do not duplicate engine logic client- or server-side — import `@black-sheep/engine/*`. Express 4 does NOT catch async-handler rejections — every async route must go through `wrap()` (server/auth.ts) or a rejection kills the process. Prisma pinned to v6 (v7 changes the generator/config model; don't bump casually). The result-card endpoints are PUBLIC by design (the link is the share) — do not "helpfully" add auth; the payload never exposes positions/cash.

## Open items
- [ ] **Phase 4 live E2E (needs DB):** two tabs, two users, same room — same live market; A's trade moves B's tape within one poll (~2.5s); close tab + reopen later → auto-resumes, market moved on. Away-summary takeover shows on rejoin.
- [ ] **USER ACTION (blocks live verification):** install a WSL distro (`wsl --install -d Ubuntu`, reboot), start Docker Desktop, then run `powershell -ExecutionPolicy Bypass -File scripts/verify-backend.ps1 -Seconds 60`. Expect: PASS verdict with MarketTick rows > 0 and growing; rival decisions appear after ~50s (engine tick 10).
- [ ] First `prisma migrate dev --name init` must run once with a live DB to generate the tracked migration (cannot be created without a shadow DB). Phase 4 added 4 nullable cols (`RivalDecisionLog`: confidence/fillPrice/realizedPnL/tickCountAt; `MarketEventLog`: tickCountAt) — all included in that same first migration.
- [ ] **Phase 4 live E2E (needs DB):** two tabs, two users, same room — same live market; A's trade moves B's tape within one poll (~2.5s); close tab + reopen later → auto-resumes, market moved on.
- [ ] `Room.status` 'ended' + end-room endpoint + world eviction (`worlds` map grows unbounded — fine for dev).
- [ ] Rival `realizedPnL` is derived from closed `Position` rows on rebuild (Fund schema has no such column — user-specced).
- [ ] The screen-record demo clip (user, once a key + DB are set).
- [x] ~~Phase 2 open items~~: RivalFund/HeadToHead duplication resolved (RivalFund Hero strip acceptable); contested-clear-on-rival-close semantics — carried into Phase 4 UI work.


## Log

### 2026-09-13 · Claude (Cline) · Phase 3 (server-authoritative liquidation + shareable result cards)
Did: Server-side liquidation — `server/world.ts` now runs `liquidatePlayerFunds(w)` during the tick loop (after `trackPeakNav`): reconstructs each player fund from DB rows, runs `checkLiquidation`, and if `atRisk`: force-closes all positions at market price, marks `Fund.liquidated = true`, computes `deriveCauseOfDeath`, looks for rival context via `computeRivalContext` (replays last 6 `RivalDecisionLog` through `detectConfrontation`), writes a `LiquidationLog` row — all in one transaction. Added `trackPeakNav` (all-time high NAV tracker) called before liquidation. New `LiquidationLog` model (roomId, fundId, startingCapital, peakNav, finalNav, cause, rivalContext, tickCountAt, durationTicks). Added `liquidated`, `peakNav` columns to `Fund`; `liquidations` relation on `Room`. New `server/resultCard.ts`: `GET /funds/:id/result-card` (public JSON) + `GET /funds/:id/result-card.png` (1200×630 PNG via `@vercel/og` `ImageResponse` with `React.createElement`). `/state` endpoint now returns `liquidated`/`liquidationCause`/`liquidationRivalContext` in `me`. Frontend: `LiquidationScreen` shows rival-context line + SHARE RESULT button; new `FundSummaryPanel` (sidebar, always accessible) with SHARE button copying the card URL; `Terminal.tsx` renders `FundSummaryPanel`. Store: new `fundId`, `name`, `liquidationCause`, `liquidationRivalContext` fields mapped from `/state`. Added `@vercel/og` + `react` deps. `prisma generate` re-run.
Left: Live E2E verification (WSL/Docker blocker); `peakNav` is updated by the tick loop + post-trade hook but the very first tick after fund creation may not capture the exact peak if the market moves before the first poll — acceptable for V1.
Watch: `computeRivalContext` looks back 6 rival decisions (≈60 ticks); a liquidation with no confrontational rival action logs `rivalContext: null` (pure leverage death). The result-card endpoints are PUBLIC — do not add auth. `@vercel/og` uses satori's default font (no custom TTF bundled) — the card is clean but not pixel-identical to the terminal's JetBrains Mono.
Next: WSL install → live E2E → watch a real fund get liquidated server-side while a rival crowds it → screenshot the result card unprompted.
Verified: `npx tsc --noEmit` PASS (root + server); `npm test` 84/84; `npm run lint` PASS; `npm run build` ✓ 1.53s; `npx prisma generate` ✓; git diff reviewed — only intended files touched.

### 2026-09-13 · Claude (Cline) · Phase 4 (frontend on the backend)
Did: Rewired the React frontend from the local Zustand simulation to the real backend. `src/store/gameStore.ts` is now a session + poll projection: `login`/`register` (auth API calls), `createRoom`/`joinRoom` (room API calls), `poll()` (GET /state every ~2.5s via Terminal.tsx interval, projects through `stateMapper.ts`, stamps `lastSeenAt`), `buy`/`short`/`closePosition` (optimistic local fills + POST /rooms/:code/trade). Fixed the store interface to declare all actions (was missing `register`, `createRoom`, `joinRoom`, `poll`, `fetchAwaySummary`, `dismissAwaySummary`, typed `confrontation` as `ConfrontationEvent | null`, `rivalFund` as `RivalFund`). Fixed cross-package imports (`STARTING_CAPITAL` from `derive`, `RIVAL_NAME` from `@black-sheep/engine/rival`, `Fund` from `@black-sheep/engine/portfolio`). New `AwaySummary.tsx` full-screen takeover ("WHILE YOU WERE AWAY") rendered over the terminal when the store holds a notable summary. Server: added `GET /rooms/:code/away-summary?since=` (`server/away.ts` + `server/awayUtil.ts`), `lastSeenAt` stamping on every `/state` poll, `lastSeenAt` column on `Fund` (Prisma). 84/84 tests pass across 7 files.
Left: Live two-tab E2E (needs DB); screen-record demo clip.
Watch: The `lastSeenAt` window for away-summary uses client-supplied `since` — the server clamps it to `now` but trusts the client's reported "last seen". A clock-skewed or tampered client could widen/narrow the window. Acceptable for V1 (the `isNotable` gate is the real protection); revisit if away-summary integrity matters.
Next: WSL install → live E2E → demo clip.
Verified: `npx tsc --noEmit` PASS (root + server); `npm test` 84/84; `npm run lint` PASS; `npm run build` ✓ 1.72s; git diff reviewed — scoped to store, App, new components, server away route, prisma.

### 2026-09-12 · Claude (Cline) · Phase 3 (backend foundation)
Did: Monorepo (`packages/engine` = `@black-sheep/engine`, workspaces + exports map; moved `src/engine` → `packages/engine/src`; all 15 frontend/test import sites updated). Additive engine changes only: `INITIAL_MARGIN_RATE` exported; `makeLLMDecision(input, endpoint?)` gains optional `{url, headers}` so the server calls Anthropic directly (browser keeps the `/api/llm` proxy — key never on the client either way). Prisma schema per spec (User/Room/Fund/Position/MarketTick/MarketEventLog/RivalDecisionLog; enums RoomStatus + PositionDirection; `@@unique([roomId,userId])`), client 6.19.3. Express server: auth (bcrypt 10 rounds, JWT 7d, ephemeral dev-secret fallback, constant-shape login errors), rooms (6-char unambiguous codes, collision retry, maxPlayers, idempotent join), state endpoint (firewall: other funds = `{id,name,userId,nav,netExposure}` only), world.ts (5s loop: engine tick + events → MarketEventLog; snapshot every 3 ticks → MarketTick `snapshotJson` = `{market, scheduler}`; rival every 10 ticks through the SAME `buildDecisionInput` → LLM/twin → `applyTrade` → `applyRivalDecision` path; state-diff persistence of cash/positions in one transaction; RivalDecisionLog rows; `rivalPending` single-flight; world rebuild from latest snapshot + closed-position realized PnL + last logged decision). `scripts/verify-backend.ps1` (db up → migrate → server → register×2 → room → join×2 → sleep → count MarketTick growth → state → verdict). `.env.example` + `docker-compose.yml` (postgres:16-alpine, healthcheck, named volume). eslint: `_`-prefix ignore (aligns with tsc convention).
Left: Live-DB verification (WSL blocker); player order endpoints; frontend integration.
Watch: `worlds` in-memory map never evicts (dev-fine); event-log timestamps are wall-clock while engine timestamps are tick units (cosmetic mismatch, noted in code); a world rebuild can lose ≤2 ticks of drift (snapshot cadence — accepted).
Next: WSL install → `scripts/verify-backend.ps1` → Phase 4 (orders + frontend on the backend).
Verified: `npx tsc --noEmit` PASS (root: src + packages); `tsc --noEmit -p server` PASS; `npm test` 75/75; `npm run lint` PASS; `npm run build` ✓ 1.65s; `npx prisma generate` OK; server boot test: `GET /health` → 200 `{"ok":true}`, world clock logged, clean shutdown via taskkill. Live DB path blocked: `wsl -l -v` → no distributions; Docker daemon down (npipe error) — documented, not worked around.

### 2026-09-12 · Claude (Cline) · Phase 2
Did: Confrontation layer — `detectConfrontation(playerPositions, rivalDecision)` (pure, 2-arg as specced) in `src/engine/rival.ts` with `ConfrontationEvent` type, 2 triggers (`opposite`, `rival_win` @ ≥$50K threshold), execution telemetry on `RivalLastDecision` (`fillPrice`/`realizedPnL`, only on real fills — unit-caught a bug where rejected trades were stamped), store state (`confrontation`, `contested`) wired into the rival resolution gated on EXECUTED actions, dedupe semantics (`opposite` once per episode, `rival_win` re-fires), cleared on player close/liquidation/restart. UI: `ConfrontationBanner` (red pulsing alert, headline + rival reasoning, 8s auto-dismiss + manual ✕), `HeadToHead` (RIVAL WATCH strip, $delta prominent, replaces the empty col-span-8), `PositionRow` `⚔ APEX` pulsing tag. RivalPanel ahead/behind block moved to HeadToHead. 13 new tests (11 unit, 2 integration).
Left: Open items (contested-clear-on-rival-close semantics; one-event banner).
Watch: `detectConfrontation` returns ONE event per rival resolution — multiple simultaneous confrontations resolve in `ticker`-find order (first match wins); the banner shows the first.
Next: Set `.env` key → play to a live banner (crowd a ticker, rival fades it) → record the Phase 2 clip.
Verified: `npx tsc --noEmit` PASS; `npm test` 75/75 (incl. confrontation unit + integration: crowd NOVA → rival shorts → banner + contested, player close clears marker, restart clears both); `npm run lint` PASS; `npm run build` ✓; git diff reviewed — only intended files touched.

### 2026-09-12 · Claude (Cline) · Phase 1
Did: Rival mechanical skeleton (`src/engine/rival.ts`: RivalFund/RivalDecisionInput/RivalDecision, information firewall, `makeRandomDecision` contrarian twin, `applyRivalDecision` via shared player engine) + LLM brain (`src/engine/rivalLLM.ts`: persona system prompt, strict-JSON contract with in-prompt example, defensive parsing incl. fence stripping, `shouldRivalReconsider` cost guard, fallback-on-failure) + store wiring (async non-blocking decision every 10 ticks, market impact on rival trades, player re-derivation after rival trades, restart/liquidation resets) + Vite plugin proxy keeping the API key server-side + RivalPanel with thinking state + 28 new tests.
Left: Duplicate rival NAV UI (see open items); real-LLM observation pending user key.
Watch: Rival can crowd itself (its own trades enter `recentFlow`) — intended shared-engine behavior for V1; revisit if the rival gets stuck in self-fading loops.
Next: Set `.env` key → play to first LLM trade → record the 15s clip.
Verified: `npx tsc --noEmit` clean; `npm test` 63/63; `npm run lint` 0 warnings; `npm run build` ok; `POST /api/llm` → key-missing 500, dummy-key → Anthropic 401 (forward proven); integration test asserts rival records a decision and marks to market.

